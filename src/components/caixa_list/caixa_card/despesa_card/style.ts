import styled from "styled-components";

export const StyledDespesaCard = styled.div`

    display:flex;
    color: var(--color-red1);
    gap:10px;

    width:100%;
;
    p{
        font-weight:400;
        font-size:.9rem;
    }
    
    `
