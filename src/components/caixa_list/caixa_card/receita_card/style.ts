import styled from "styled-components";

export const StyledReceitaCard = styled.li`

    display:flex;
    flex-direction:row;
    color: var(--color-green1);
    gap:10px;


    p{
        font-weight:400;
        font-size: .9rem;
    }
    
    `
